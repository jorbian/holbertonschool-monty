#include "monty.h"

/**
* pint - Swap top two values of stack
* @stack: Pointer to the top mode node of stack.
* @line_number: Current line number.
*/
void pint(Sstack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
