#include "monty.h"

/**
 * pop - Removes top element from stack.
* @stack: Pointer to the top mode node of stack.
* @line_number: Current line number.
*/
void pop(Sstack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
